{
  "name": "premium-expense-tracker-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Premium full-stack expense tracker dashboard built with React, Express, MongoDB, JWT auth, analytics, exports, and SaaS-grade UI.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "audit:prod": "npm audit --omit=dev --prefix backend && npm audit --omit=dev --prefix frontend"
  },
  "keywords": [
    "expense-tracker",
    "react",
    "express",
    "mongodb",
    "dashboard",
    "saas"
  ],
  "author": "",
  "license": "MIT"
}
